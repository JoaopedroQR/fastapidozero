name: pipeline
on: [push, pull-request]

jobs:
  test:
    runs-on: ubuntu-latest


    steps:
      - name: instalar o python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'